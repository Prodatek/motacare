apiVersion: apps/v1
kind: Deployment
metadata:
  name: vehicle-service
  namespace: motacare
  labels:
    app: vehicle-service
    version: "1.0.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vehicle-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: vehicle-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: vehicle-service
          image: REPLACE_WITH_ECR_URI/motacare-vehicle-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3002
          envFrom:
            - configMapRef:
                name: motacare-config
            - secretRef:
                name: motacare-secrets
          env:
            - name: PORT
              value: "3002"
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: motacare-config
                  key: VEHICLE_DB_NAME
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 20
            periodSeconds: 15
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: vehicle-service
  namespace: motacare
spec:
  selector:
    app: vehicle-service
  ports:
    - port: 3002
      targetPort: 3002
