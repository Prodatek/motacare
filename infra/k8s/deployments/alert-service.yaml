apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-service
  namespace: motacare
  labels:
    app: alert-service
    version: "1.0.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alert-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: alert-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: alert-service
          image: REPLACE_WITH_ECR_URI/motacare-alert-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3006
          envFrom:
            - configMapRef:
                name: motacare-config
            - secretRef:
                name: motacare-secrets
          env:
            - name: PORT
              value: "3006"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 20
            periodSeconds: 15
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: alert-service
  namespace: motacare
spec:
  selector:
    app: alert-service
  ports:
    - port: 3006
      targetPort: 3006
