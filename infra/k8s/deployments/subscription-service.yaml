apiVersion: apps/v1
kind: Deployment
metadata:
  name: subscription-service
  namespace: motacare
  labels:
    app: subscription-service
    version: "1.0.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: subscription-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: subscription-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: subscription-service
          image: REPLACE_WITH_ECR_URI/motacare-subscription-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3007
          envFrom:
            - configMapRef:
                name: motacare-config
            - secretRef:
                name: motacare-secrets
          env:
            - name: PORT
              value: "3007"
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: motacare-config
                  key: SUBSCRIPTION_DB_NAME
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 20
            periodSeconds: 15
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: subscription-service
  namespace: motacare
spec:
  selector:
    app: subscription-service
  ports:
    - port: 3007
      targetPort: 3007
